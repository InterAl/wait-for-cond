{
  "name": "wait-for-cond",
  "version": "1.0.2",
  "description": "Returns a promise that resolves when a condition is met, and rejects when hitting a timeout",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/InterAl/wait-for-cond"
  },
  "keywords": [
    "wait",
    "for",
    "condition"
  ],
  "author": "InterAl",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^3.2.0"
  }
}
